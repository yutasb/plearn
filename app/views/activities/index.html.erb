<h1>マイページ</h1>
<%= link_to '新規登録', new_activity_path, class:'btn btn-primary' %>



<br><br>
<h4>一覧</h4>
<% @activities.each do |activity|%>
    <%= link_to "#{activity.title}", activity_path(activity) %> 
    <%= link_to '編集', edit_activity_path(activity), class:'btn btn-warning' %>
    <br>
<% end%>
